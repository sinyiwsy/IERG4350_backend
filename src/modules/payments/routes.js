import {loadStripe} from '@stripe/stripe-js';

// export default (server, options, next) => {
//   server.post("/payment", { schema: postUsersSchema }, async (req, res) => {
    
//     res.code(201).send(user);
//   });
//   next();
// };
